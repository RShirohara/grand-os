chore: bump GrandOS image version to `v${VERSION}`

Bump GrandOS image version to `v${VERSION}`.

## Changelog

${COMMITS}

## Notes

This pull request was created by workflow triggered by `${EVENT_NAME}`.
Additional info:
> ${ADDITIONAL_INFO}
